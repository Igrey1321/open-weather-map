export type cityWeatherState = {
  id: number;
  name: string;
  weather: string;
  temp: number;
  lat: number;
  lon: number;
};

export type LocalGeocodingType = {
  id: number;
  lat: number;
  lon: number;
};
